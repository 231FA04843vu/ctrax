# Server notifications API configuration (Fly.io or local)

# Option A: Base64-encoded Firebase service account JSON (recommended for Fly.io secrets)
# FIREBASE_SERVICE_ACCOUNT=

# Option B: Path to service account JSON file (for local dev)
# FIREBASE_SERVICE_ACCOUNT_PATH=server/ctrax.json

# Optional API key to protect /api/notify endpoint
# NOTIFY_API_KEY=

# Port for the Node server (Fly.io Dockerfile defaults to 8080)
PORT=8080
# Firebase Admin credentials for FCM (choose one method)
# 1) Base64-encoded service account JSON (recommended for .env files)
// To generate in PowerShell:
//   $bytes = [System.IO.File]::ReadAllBytes("C:\\secure\\service-account.json")
//   [Convert]::ToBase64String($bytes)
// Paste the base64 string below:
# FIREBASE_SERVICE_ACCOUNT=

# 2) Path to service account JSON file
# FIREBASE_SERVICE_ACCOUNT_PATH=./service-account.json

# Optional API key to protect /api/notify endpoint (recommended)
# If empty, endpoint is left minimally restricted (for local testing only)
# NOTIFY_API_KEY=your-strong-random-key

# Server port (default 3000)
# PORT=3000
